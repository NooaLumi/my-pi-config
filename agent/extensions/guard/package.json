{
   "name": "guard",
   "version": "0.1.0",
   "license": "MIT",
   "type": "module",
   "scripts": {
      "check": "biome check --write --error-on-warnings . && tsgo --noEmit"
   },
   "keywords": [
      "pi-package"
   ],
   "author": "Nooa Lumilaakso",
   "dependencies": {
      "shell-quote": "^1.8.3"
   },
   "devDependencies": {
      "@types/shell-quote": "^1.7.5"
   }
}
